[build]
builder = "NIXPACKS"
buildCommand = "dotnet restore && dotnet publish -c Release -o out"

[deploy]
startCommand = "dotnet out/EcommerceAPI.dll"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3